{
    "version": 2,
    "name": "cat_app",
    "builds": [{ "src": "cat_app/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "cat_app/$1" }
    ],

    "env":{
        "REACT_APP_API_KEY": "@catappapikey"
    }
}